/**
 * Bundled mark URL (hashed under `/assets/` in production) for favicon + sidebar.
 * The sidebar tile uses `object-fit: cover` so the square is filled; very wide marks may crop at the sides.
 * A **square** mark (~512×512) avoids cropping; favicon use is unchanged.
 */
import flightdeckMarkUrl from "./assets/flightdeck-icon.png";
export { flightdeckMarkUrl };
