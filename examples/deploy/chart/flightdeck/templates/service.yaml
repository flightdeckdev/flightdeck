apiVersion: v1
kind: Service
metadata:
  name: {{ include "flightdeck.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "flightdeck.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "flightdeck.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
